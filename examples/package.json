{
  "name": "elm-firebase-dict-demo",
  "version": "0.0.1",
  "description": "elm firebase dict sync demo",
  "main": "index.html",

  "scripts": {

    "build" : "elm-make Main.elm --output=main.js",
    "live" : "elm-live Main.elm --output=main.js --debug --open --pushstate --port=8008",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
  },

  "author": "Roberto Tazzoli",
  "license": "MIT"

}
